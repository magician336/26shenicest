

## Codely Structured Memories

### User

### Feedback
- [2026-08-27 21:39:39] User prefers to debug directly in the Game scene, skipping the main menu / session creation flow. **Why:** Faster iteration during hackathon; doesn't want to deal with networking setup just to test gameplay. **How to apply:** When building debug/test tooling, prioritize direct Game scene entry over full flow simulation.
- [2026-08-27 23:13:56] DraggableItem drag animation fix (2026-08-27): Original code used ScreenPointToLocalPointInRectangle against Canvas root, but draggable items are nested under _content → Panel → Canvas. Coordinate space mismatch caused cards not to follow mouse. Fix: On OnBeginDrag, reparent to Canvas root temporarily; in OnDrag use ScreenToWorldPoint to set position directly. **Why:** Nested RectTransform hierarchy causes local-to-screen coordinate conversion failures when the conversion target rect doesn't match the draggable's actual parent. **How to apply:** For UI drag-and-drop in deeply nested Canvas hierarchies, reparent to Canvas root during drag and use world-space positioning instead of local-space conversion.
- [2026-08-28 00:51:31] MiniGameBase.EndGame() must destroy its _content GameObject (the private UI container parented under Panel). **Why:** MiniGameManager.DestroyActiveView destroys the MiniGame MonoBehaviour's own GameObject but _content is parented to Panel (a sibling), so without EndGame cleanup old UI persists and overlaps the next mini-game's UI when canvas re-activates. **How to apply:** Any MiniGameBase subclass that creates a _content under Panel must Destroy(_content) in EndGame override. This applies to CookingMiniGame and BaguaMiniGameView.
- [2026-08-28 22:54:17] IntroCutsceneController Text invisible bug (2026-08-28): Text.color.a was initialized to 0f, but CrossFadeAlpha only controls CanvasRenderer.alpha — the final render alpha = color.a × canvasRenderer.alpha = 0 × 1 = 0. Fix: set color.a=1f and let CrossFadeAlpha handle the fade. **Why:** Unity UI Text alpha is the product of CanvasRenderer.alpha and Graphic.color.a; setting only CanvasRenderer.alpha is not enough. **How to apply:** When using CrossFadeAlpha on Text/Image, always ensure color.a=1 (or your desired max), then animate CanvasRenderer.alpha via CrossFadeAlpha.
- [2026-08-29 16:10:29] DeskViewController.Awake() plays a full-screen black Canvas overlay (awakening sequence ~11s) on first scene load, blocking all scene visibility in Play Mode. **Why:** It's a story cutscene that triggers when PlayerPrefs "DeskView_AwakeningShown" is 0 (first play). **How to apply:** Before any Play Mode testing of LivingRoom scene, set PlayerPrefs.SetInt("DeskView_AwakeningShown", 1) in Edit Mode via exec_editor_script, otherwise the Game View will be entirely black for ~11 seconds.

### Project
- [2026-08-27 22:51:32] Local debug network layer added (2026-08-27): LocalDebugService + LocalGameplayBridge + LocalNetworkBootstrap in Network/Local/, auto-registered when FUSION_PRESENT is not defined. Enables single-process Game scene debugging with Tab key role switching. debugSingleProcess flag on SessionGameplayCoordinator bypasses Host authority checks. **Why:** Fusion SDK not yet imported, user needed to test full mini-game flow locally. **How to apply:** This is a debug-only layer; importing Fusion and defining FUSION_PRESENT automatically disables it via conditional compilation. See docs/local-mock-network.md.
- [2026-08-29 17:19:54] - [2026-08-27 22:51:32] Bagua mini-game implemented (2026-08-27, updated 2026-08-29): Dual mini-game architecture using GameplaySnapshot. Bagua flow updated: Kitchen DoorToCourtyard(F, requires photo_hongqiang+photo_hongfang) → Courtyard sub-page (CourtyardEavesdropView) → "进入偷听" button → StartBaguaMiniGame → Client hears stories + matches items → Host photo identification → 完成 → "退出偷听" button → SceneLoader.Load(Kitchen). DoorListeningTrigger.cs no longer used in Courtyard scene (was the old F-key trigger with distance audio). Config: 3 characters (liu_hongxiu/key, liu_hongju/tin_candy_box, liu_hongbin/wooden_cart) + 3 photo zones. BaguaStoryConfig now has photoZoneSprite + nameTagSprite fields (from session patch).

- [2026-08-27 23:13:56] MiniGameManager.OnStateChanged was using old CookingGameState signature but project had been refactored to GameplaySnapshot (2026-08-27). Fixed to accept GameplaySnapshot and extract .cooking sub-state. LocalGameplayBridge also updated for GameplaySnapshot. **Why:** Project was refactored externally (Bagua added) but MiniGameManager wasn't updated to match. **How to apply:** When IGameplayTransport or SessionGameplayCoordinator changes its state type, check all subscribers (MiniGameManager, LocalGameplayBridge, FusionGameplayBridge) for signature compatibility.
- [2026-08-28 00:51:31] Multi-recipe cooking system added (2026-08-28): CookingMiniGame is fully data-driven from RecipeConfig (seasoningOptions, recipeNote, containerDisplayName, nextRecipeId fields). Two recipes: tomato_egg (reward: photo_hongqiang) → cucumber_salad (reward: photo_hongfang). Chain flow: FinishMiniGame stores _nextRecipeAfterPhoto → returns to Exploration → MemoryAlbum shows photo pickup → Host collects → preview opens → Host closes preview → CloseMemoryPhotoPreview auto-starts next recipe via StartMiniGame. **Why:** Design doc requires sequential dishes where player collects photo reward between dishes. **How to apply:** Add new recipes by creating RecipeConfig assets + wire into coordinator.recipes and MiniGameManager.recipes. Use `Tools > 3C Setup > Update Recipe Assets` to create/update recipe assets and wire them into the open scene without full scene rebuild. RecipeNote (菜谱改痕) shows only on mother's side after cooking completes, never during ingredient selection or on daughter's side.
- [2026-08-28 02:05:34] PlayerInputHandler locks movement when pendingPhotoId or previewPhotoId is non-empty (2026-08-28). **Why:** Design requires player to collect photo rewards before resuming exploration. **How to apply:** If player is stuck after completing a mini-game, check Console for `[PlayerInput] 移动被锁定` log — it means pendingPhotoId is set but the photo pickup button in MemoryAlbumController may not be visible or interactable (e.g. role mismatch in LocalDebug mode where ShowPickup created button with interactable=false and early-return prevented re-enabling). Fixed by updating interactable state on existing pickup when role changes.
- [2026-08-28 21:49:01] [FIXED 2026-08-28] Bug in SessionGameplayCoordinator.FinishMiniGame bagua branch: `State.miniGameId` was set to null BEFORE `FindBaguaConfig(State.miniGameId)` was called, so `_nextDialogueAfterPhoto` for bagua was always null. Fix applied: `FindBaguaConfig(State.miniGameId)` now called before `State.miniGameId = null`.

- [2026-08-28 18:58:31] [2026-08-28] Bagua Client-side UI redesigned (2026-08-28): Replaced nine-grid item matching with flat desktop layout (top: task banner, middle: wooden tray with scattered draggable items, bottom: 3 character cards). Each card has portrait + name/age·title + audio button + dashed drop slot. Must hear story (audio or subtitle fallback) before slot activates. Items scatter by ItemPlacement.anchoredPosition (configured in BaguaStoryConfig). Data model: BaguaStoryEntry lost correctItemId/itemSprite, gained age/title. New ItemPlacement struct (itemId, displayName, sprite, anchoredPosition, isCorrect, characterId) replaces distractorItemIds + correctItemId. 8 items total (3 correct + 5 distractors). Subtitle bar: bottom popup, duration = len/5+1s, audio-present suppresses subtitle. Matched item: scale-pop animation fills slot. Wrong drop: ReturnToOrigin + ShakeRect. No interrupt button on Client side. **Why:** Design doc requires scattered-item drag matching on a nostalgic desktop, not a grid. **How to apply:** Old BaguaStoryConfig.asset deleted — run Tools > 3C Setup > Create Basic Scene to regenerate with new structure. CONTEXT.md created at project root for domain terminology.
- [2026-08-28 21:27:44] [2026-08-28] Album mini-game (game 3) implemented (2026-08-28): Independent third MiniGameBase subclass (AlbumMiniGameView) in MiniGame/Album/. Same-screen co-op — both Host and Client see identical UI and can drag; Host-authoritative via PlaceAlbumSticker/PlaceAlbumNameTag intents (CanRequesterPerform returns true for both roles). Two-phase serial: PlaceStickers (5 stickers → 6 silhouette zones, liu_hongmei/小岩 has no sticker, always empty) → PlaceNameTags (5 name tags → name tag zones on placed stickers) → Complete button → full-screen realistic family portrait (CrossFadeAlpha) → fade to black → GameEnded phase. New GameplayPhase.GameEnded (game body ended, awaiting epilogue content). AlbumTrigger at game start position (0, 0.5) checks collectedPhotoIds contains all 3 required photos (photo_hongqiang, photo_hongfang, bagua_old_family_photo) before allowing entry; shows lockedMessage if not. Clue viewer: left-top "查看线索" button → full-screen panel with photo + yellow sticky note (clueText from AlbumConfig). No hint system (oral communication only). AlbumConfig has 6 AlbumCharacterEntry (characterId, displayName, stickerSprite, photoSprite, clueText, stickerZone/NameTagZone positions+sizes, hasSticker). AlbumGameState: placedStickerCharacterIds, placedNameTagCharacterIds, completed. Bagua game unchanged (HostIdentifyPeople stays). Pre-existing BaguaMiniGameView KeyValuePair==null bug also fixed. **Why:** Design requires a terminal album assembly game using photos collected from cooking+bagua, with 小岩's empty silhouette leading to epilogue. **How to apply:** Run Tools > 3C Setup > Create Basic Scene to regenerate scene with AlbumConfig.asset + AlbumTrigger. Epilogue flow (BlackScreen+text+AIGC video) is a reserved interface — FinishMiniGame sets phase=GameEnded, MiniGameManager shows black overlay.
- [2026-08-28 21:41:48] [project] 4-scene orchestration code changes (2026-08-28): SessionGameplayCoordinator + MiniGameManager now use DontDestroyOnLoad for cross-scene persistence. Coordinator uses SceneManager.sceneLoaded to rebuild dependencies (MemoryAlbumController, DialogueController) after scene transitions. New SceneNames.cs constants class (Core/). Cross-scene transition: CloseMemoryPhotoPreview checks _cookingChainCompleted flag → TransitionToScene(BaguaScene) with Build Settings existence guard. GameManager.HandleSceneLoaded cleans up persistent objects when returning to MainMenu. LocalDebugService.StartHost/StartClient no longer hard-loads Game scene if already in a game scene. FusionSessionService loads Intro (fallback to Cooking) on connect. **Why:** Preparing for 4-scene split (MainMenu/Intro/CookingScene/BaguaScene) without touching .unity files yet. **How to apply:** When creating the actual scene files, update SceneNames.Cooking constant from "Game" to "CookingScene" and ensure BaguaScene is in Build Settings — the TransitionToScene guard will auto-activate.
- [2026-08-28 21:49:01] [2026-08-28] FusionGameplayBridge signature mismatch FIXED (2026-08-28): `BroadcastState(CookingGameState)` → `BroadcastState(GameplaySnapshot)` to match IGameplayTransport interface. `RpcApplyState` deserialization changed from `CookingGameState` to `GameplaySnapshot`. Removed unused `using DoNotForgetMe.MiniGame.Cooking`. Still under `#if FUSION_PRESENT` (not compiled in LocalDebug mode).
- [2026-08-28 21:49:01] [2026-08-28] PhotonAppSettings.asset FixedRegion set to "asia" (Singapore, best latency for mainland China). AppIdFusion already configured (ecb11ee8-...). Only relevant when FUSION_PRESENT is defined.
- [2026-08-29 18:00:33] [project] Cinematic letterbox scene layout v5 (2026-08-29): Constants: SCENE_VIEW_HEIGHT=7.0, CAMERA_FIXED_Y=-0.5, CAMERA_ORTHO_SIZE=5, BG_CENTER_Y=-0.5, PLAYER_Y=-2.0 (was -1.5, feet≈-3.0 on bg ground), SUBTITLE_BAR_Y=-5.0 (was -5.5, visible in camera view), FIXED_ASPECT=16f/9f. Player: SimpleWalkAnimation PPU=466 (walk frames 2048×1024, char pixels 268×931, world height=2.0). Collider 0.6×1.8 (was 0.8×0.8). SubtitleText fontSize=3.0 (was 2.5). All trigger Y use PLAYER_Y constant. **CRITICAL BUG FIX**: AudioManager.DontDestroyOnLoad in CreateCoordinator throws InvalidOperationException in Edit Mode → scene generation aborts after Coordinator (MiniGameManager/DeskVC/Door/Spawn all missing). Removed DontDestroyOnLoad call from CreateCoordinator (Awake handles it at runtime). CourtyardEavesdropView: removed UI bg Image (was causing double background with world-space SpriteRenderer). Spec doc v2 at docs/scene-layout-spec.md.







- [2026-08-29 17:19:17] [project] 5-scene orchestration updated (2026-08-29): Flow is MainMenu → Intro → LivingRoom → Kitchen → Courtyard(子页面) → Kitchen → LivingRoom(album) → GameEnded. Two transition types: (1) Passive — Coordinator _cookingChainCompleted → Courtyard, via CloseMemoryPhotoPreview. _baguaChainCompleted no longer auto-transitions (CourtyardEavesdropView button handles exit). (2) Active — SceneTransitionTrigger (IInteractable) for door F-key transitions. Courtyard is now a sub-page (CourtyardEavesdropView) with no Player/exploration. Files: IntroCutsceneController.cs, SceneLoader.cs, SceneTransitionTrigger.cs, CourtyardEavesdropView.cs (new). SceneNames: LivingRoom/Kitchen/Courtyard. DeskViewController: F to re-interact (photos collected → album game; not collected → desk view).

- [2026-08-28 23:42:02] Photo reward timing fix (2026-08-28): QueueRewards moved from SelectSeasoning/AssignBaguaPhotoName (called mid-minigame) to FinishMiniGame (called after minigame ends). CookingMiniGame.RenderDaughterView now shows a "获得照片" card inside the minigame canvas when state.completed (during the 1.4s display window before FinishMiniGame). Previously pendingPhotoId was set during the minigame, causing the "新照片 点击收集" button to appear in the exploration HUD (bottom-right) while the minigame was still active. **Why:** Design requires photo reward to display inside the minigame completion view, not in the kitchen scene corner. **How to apply:** Any new mini-game that awards photos should queue rewards in FinishMiniGame, not in the completion handler.
- [2026-08-29 13:14:23] Unity 项目实际路径在 Git 仓库子目录下。当前活跃项目在 `D:\26shenicest-main\DoNotForgetMe\New Tuanjie Project\`（队友的完整副本，含 Art 资产）。原始副本在 `D:\26shenicest\DoNotForgetMe\New Tuanjie Project\`（无 Art 资产）。Git 根目录 `D:\26shenicest` 和 `D:\26shenicest-main` 的 Assets/ 只有 Photon/，没有场景和脚本。场景文件在 `Assets/_Project/Scenes/`。美术资产在 `Assets/_Project/Art/`（Backgrounds/Characters/Items/Photos/Seasonings/AnimationFrames/Resources/WalkFrames）。**Why:** Git 仓库根目录和 Unity 项目目录不一致，Codely CLI 从 Git 根启动但 Unity 项目在子目录。**How to apply:** 打开 Unity 时必须选择子目录项目；Codely CLI 连接子项目编辑器的方法：将子项目的 `.com-unity-codely.json` 复制到根目录，再调用 unity_refresh。注意不要同时开两个 Unity 编辑器实例，否则 Codely 会连到错误的项目。
- [2026-08-29 15:00:03] UI 动效系统 v3 (2026-08-29): 三层架构 UiTween.cs (8种缓动函数+Scale/Move/FadeAlpha[Graphic]/FadeColor/Sequence/Parallel) + ButtonHoverEffect.cs (hover1.06x+press0.92x+颜色微亮, Start阶段防止scale=0缓存) + UiFx.cs (ShrinkOut[EaseInCubic+旋转+淡出]、SteamBurst[柔和渐变圆Sprite,startY修复,上飘+Sine摇摆+延迟+渐隐]、LightBurst[中心闪光+星形菱形射线+旋转飞散]、CardFlip[EaseInCubic前半段→EaseOutBack后半段,0.6s,中点回调切换可见性]、PulseGlow[Alpha+localScale双脉动]、PopIn)。DraggableItem: 弹性放大1.18x+渐变阴影Sprite+倾斜跟随。CookingMiniGame: renderKey防重复渲染+食材PopIn+缩小蒸汽入锅+CardFlip照片翻转+LightBurst光芒+PulseGlow收集按钮+Text Alpha渐入+持续环境蒸汽(AmbientSteam,每1.2s一波4粒子)。**已修复BUG:** (1)SteamBurst startY未赋值→已传offsetY (2)ShrinkOut EaseInBack导致镜像→改EaseInCubic (3)CardFlip EaseInBack先放大→改EaseInCubic (4)ButtonHoverEffect在scale=0时AddComponent缓存zero→延迟AddComponent。**已知架构问题:** ClearContent()全量销毁会杀死动画协程——Text渐入(0.3s)在renderKey变化时被ClearContent杀死; 测试脚本直接发Intent绕过UI拖拽路径,无法触发DropIngredientFx; SetRole不触发StateChanged需手动republish。**How to apply:** Text入场建议用scale PopIn而非alpha渐入(抗ClearContent); 蒸汽/光芒粒子的parent必须传Panel; 实际效果验证需手动Play+拖拽,程序化测试无法覆盖UI事件驱动动效。
- [2026-08-29 18:00:39] Walk animation update (2026-08-29): 35 frames (frame_0001~frame_0035) at 2048×1024 res, loaded via Resources.Load from "WalkFrames/frame_" path. Character pixel bounds: 268×931 (90.9% of frame height). Sprite.Create PPU=466 (was 200→too big, 800→too small; 466 gives char height=2.0 world units). 3 places must be updated (including !isReadable fallback path). Frames face LEFT by default — flipX logic: horizontalInput > 0 (walking right) → flipX=true. SimpleWalkAnimation.cs auto-detects non-transparent pixel bounds for centered pivot.

- [2026-08-29 16:24:50] [project] 音频系统实现 (2026-08-29): AudioManager 单例 (DontDestroyOnLoad) + AudioLibrary ScriptableObject + SfxId/BgmId/AmbId 枚举。音频文件在 Assets/_Project/Audio/{Voice,SFX,BGM,AMB}/。Voice/BGM/AMB 用 Streaming loadType, SFX 用 DecompressOnLoad。AudioLibrary.asset 在 Assets/_Project/Settings/。调用方式: AudioManager.Play(SfxId.X) / PlayBgm(BgmId.X) / PlayAmb(AmbId.X)。SceneSetupBase.CreateCoordinator 末尾自动创建 AudioManager 并接线 library。
- [2026-08-29 16:24:50] [project] 对话音频与字幕系统 (2026-08-29): CinematicSubtitle.Show(text, speaker, audioClip, duration) 统一入口，格式【角色：台词】，AudioSource 同步播放。DialogueController 移除自有 Canvas UI，改用 CinematicSubtitle + 全屏透明 ClickOverlay (Host 点击推进)。4 个 DialogueSequence.asset 在 Assets/_Project/Dialogue/: DLG_EnterMemory(开场HX01), DLG_Game1ToGame2(做饭后HQ01+HF01+HM1501+HB01), DLG_Game2ToGame3(八卦后HJ01+HQ03+HX03+HX02), DLG_Ending(HM1505)。链路: CucumberSaladRecipe.nextDialogueId=DLG_Game1ToGame2, BaguaStoryConfig.nextDialogueId=DLG_Game2ToGame3, Coordinator.openingDialogueId=DLG_EnterMemory。八卦故事音频 HM1502/03/04 已写入 BaguaStoryConfig.entries.storyAudio。
- [2026-08-29 16:28:34] Session patch 合入 (2026-08-29): 队友提供的 session_patch.zip 含 9 张美术资产 + 4 个脚本变更，已全部合入。改动: (1) BaguaStoryConfig.cs 新增 photoZoneSprite 字段+属性、BaguaStoryEntry 新增 nameTagSprite 字段（直接用补丁版替换，是超集）。(2) BaguaMiniGameView.cs — CreatePhotoDropZone 支持 PhotoZoneSprite 图标（非null时显示图标替代纯色），CreateNameTag 签名新增 nameTagSprite 参数（非null时显示图标替代文字标签）。(3) PlayerController.cs 交互提示 scale 0.05→0.1 放大2倍、rotation -45°→identity 去倾斜。(4) SceneSetupBase.cs 新增 NAMETAG_HONGXIU/HONGJU/HONGBIN_PATH + PHOTO_ZONE_SPRITE_PATH 常量 + WireArtAssets 中接线逻辑。**坑:** 新增 PNG 文件的 TextureImporter 默认不是 Sprite 类型，AssetDatabase.LoadAssetAtPath<Sprite>() 返回 null，必须先设置 textureType=Sprite + spriteImportMode=Single 并 SaveAndReimport 后才能加载为 Sprite。WireArtAssets 的 LoadSprite 方法只对已是 Sprite 类型的文件有效。**How to apply:** 以后任何新增 PNG 美术资产，先通过 exec_editor_script 检查并修复导入器设置，再运行 Wire Art Assets。
- [2026-08-29 17:19:08] Courtyard 重构为子页面 (2026-08-29): Courtyard 不再是探索场景，改为 CourtyardEavesdropView 子页面。新文件: Cutscene/CourtyardEavesdropView.cs — 全屏背景(SerializeField backgroundSprite, 场景生成时接线) + "进入偷听"按钮(触发 StartBaguaMiniGame) + "退出偷听"按钮(bagua完成后出现, 点击 SceneLoader.Load(Kitchen))。监听 Coordinator.StateChanged 自动切换按钮显隐。CourtyardSceneSetup 重写: 移除 Player/DoorListeningTrigger/DoorToLivingRoom/PlayerSpawn, 只保留 Camera+AudioListener+背景+字幕条+CourtyardEavesdropView。SessionGameplayCoordinator._baguaChainCompleted 不再自动 TransitionToScene(LivingRoom), 改为仅 PublishStableState(), 退出由 CourtyardEavesdropView 按钮手动触发。Kitchen DoorToCourtyard 添加 requiredPhotoIds={photo_hongqiang, photo_hongfang}。**Why:** Courtyard 只是一个偷听入口, 不需要角色行走和探索。**How to apply:** 新流程: Kitchen做完结菜 → 门(→Courtyard) → Courtyard子页面(全屏背景+进入偷听按钮) → 八卦小游戏 → 完成 → 退出偷听按钮 → 回Kitchen。AudioListener 必须在 CreateCamera 中添加(camObj.AddComponent<AudioListener>()), 否则所有音频静默失败。
- [2026-08-29 17:34:27] [project] 线性流程跳转逻辑修正 (2026-08-29): DialogueSequence 新增 nextSceneName 字段, FinishDialogue() 末尾检查 nextSceneName → TransitionToScene 实现对白播完后自动转场。DLG_Game1ToGame2.nextSceneName="Courtyard", DLG_Game2ToGame3.nextSceneName="LivingRoom"。DLG_Game2ToGame3 对白顺序按 spec 修正为 HX02→HJ01→HQ03→HX03。InitializeSessionState 新增: collectedPhotoIds.Count>=3 且 phase==Exploration → 自动 StartAlbumMiniGame (不再需要 DeskView F 交互)。CourtyardEavesdropView 移除"退出偷听"按钮 (八卦完成后对白自动转场)。EndingController.cs 新建: 4幕结尾文本+3句公益字幕, 由 MiniGameManager.ShowGameEndedOverlay 创建。验证通过: 做饭完成→对白→自动转场Courtyard (Console确认 SceneLoader.Load("Courtyard"))。

### Reference
- [2026-08-29 13:14:26] Tuanjie Editor 1.6.12 installed at D:\Program Files\Tuanjie 2022.3.61t13\Editor\Tuanjie.exe. Batch mode (PowerShell): `& "D:\Program Files\Tuanjie 2022.3.61t13\Editor\Tuanjie.exe" -batchmode -nographics -projectPath <path> -logFile <path> -quit` — note the `&` call operator is required in PowerShell. **Gotcha:** if code changes trigger recompilation during batch mode, assembly reload causes -quit to fire before executeMethod runs. Run twice: first to compile, second to execute. **Another gotcha:** batch mode fails with "Fatal Error! It looks like another Tuanjie instance is running" if the Editor GUI is already open — close it first. Must remove FUSION_PRESENT define (and all FUSION_* macros) from ProjectSettings.asset when Fusion SDK is not imported, otherwise FusionSessionService.cs fails to compile. **Note:** Fusion SDK IS present in Assets/Photon/Fusion/ — but FUSION_PRESENT is NOT defined, so LocalDebugService runs instead. The project has a pre-existing Fusion NetworkProjectConfig.fusion asset conflict that throws a non-fatal error on every editor launch — ignore it.


- [2026-08-28 17:31:31] [reference] 程序交付文档位于 docs/程序交付文档.md (created 2026-08-28)。覆盖架构、56脚本清单、数据驱动配置、场景结构、游戏流程、网络层、存档系统、运行验证、缺失资源与接入指南。
- [2026-08-29 13:14:29] `Tools > 3C Setup > Wire Art Assets`（SceneSetupBase.WireArtAssets()）是将 Art/ 目录下美术资产批量接线到 ScriptableObject 配置的标准方法。自动设置 TextureImporter 为 Sprite 模式，然后将 Sprite 引用写入 RecipeConfig（食材/调料/奖励/容器/背景/菜图）、BaguaStoryConfig（人物立绘/物件/老照片/桌面背景/听按钮/女儿端背景）、AlbumConfig（贴纸/照片/写实全家福）。新增美术资产后只需重新执行即可。
