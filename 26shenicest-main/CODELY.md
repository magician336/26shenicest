

## Codely Structured Memories

### User

### Feedback
- [2026-08-27 21:39:39] User prefers to debug directly in the Game scene, skipping the main menu / session creation flow. **Why:** Faster iteration during hackathon; doesn't want to deal with networking setup just to test gameplay. **How to apply:** When building debug/test tooling, prioritize direct Game scene entry over full flow simulation.
- [2026-08-27 23:13:56] DraggableItem drag animation fix (2026-08-27): Original code used ScreenPointToLocalPointInRectangle against Canvas root, but draggable items are nested under _content → Panel → Canvas. Coordinate space mismatch caused cards not to follow mouse. Fix: On OnBeginDrag, reparent to Canvas root temporarily; in OnDrag use ScreenToWorldPoint to set position directly. **Why:** Nested RectTransform hierarchy causes local-to-screen coordinate conversion failures when the conversion target rect doesn't match the draggable's actual parent. **How to apply:** For UI drag-and-drop in deeply nested Canvas hierarchies, reparent to Canvas root during drag and use world-space positioning instead of local-space conversion.
- [2026-08-28 00:51:31] MiniGameBase.EndGame() must destroy its _content GameObject (the private UI container parented under Panel). **Why:** MiniGameManager.DestroyActiveView destroys the MiniGame MonoBehaviour's own GameObject but _content is parented to Panel (a sibling), so without EndGame cleanup old UI persists and overlaps the next mini-game's UI when canvas re-activates. **How to apply:** Any MiniGameBase subclass that creates a _content under Panel must Destroy(_content) in EndGame override. This applies to CookingMiniGame and BaguaMiniGameView.
- [2026-08-28 22:54:17] IntroCutsceneController Text invisible bug (2026-08-28): Text.color.a was initialized to 0f, but CrossFadeAlpha only controls CanvasRenderer.alpha — the final render alpha = color.a × canvasRenderer.alpha = 0 × 1 = 0. Fix: set color.a=1f and let CrossFadeAlpha handle the fade. **Why:** Unity UI Text alpha is the product of CanvasRenderer.alpha and Graphic.color.a; setting only CanvasRenderer.alpha is not enough. **How to apply:** When using CrossFadeAlpha on Text/Image, always ensure color.a=1 (or your desired max), then animate CanvasRenderer.alpha via CrossFadeAlpha.
- [2026-08-29 19:30:07] Prefabricated prefab exporters must use GetComponent<RectTransform>() not AddComponent<RectTransform>() when the GameObject was already created with typeof(RectTransform). Also StretchRect must explicitly set anchoredPosition=Vector2.zero and sizeDelta=Vector2.zero, not just offsetMin/offsetMax — Unity leaves residual anchoredPosition after stretch-anchor conversion (e.g. MotherPanel was offset 168px). **Why:** Direct file edits to prefab YAML via replace tool are error-prone; the exporter must produce correct values programmatically. **How to apply:** Any future prefab exporter (Cooking/Bagua/Album pattern) must follow these rules.
- [2026-08-29 21:59:23] [feedback] When migrating to prefab-based instantiation, do NOT add code constants (e.g. PLAYER_SCALE, GROUND_POS) that override Transform values after InstantiatePrefab — this defeats the purpose of making things editable in the Inspector. Let the Prefab be the single source of truth for position/scale. **Why:** User asked "为什么生成的scene无法在unity编辑器中直接改" — code overrides were silently clobbering Inspector edits, making prefab changes appear to not work. **How to apply:** After PrefabUtility.InstantiatePrefab, only set properties that genuinely need to be computed at runtime (e.g. ground width from bgWorldWidth). Never hardcode position/scale that the user should be able to edit in the prefab.
- [2026-08-29 22:23:10] CreateDefaultSprite must use ≥64×64 texture, not 4×4 (2026-08-29). A 4×4 white texture scaled hundreds of times by SpriteRenderer produces a visible white crosshair line in Game view due to UV seam artifacts, even with FilterMode.Point. Fix: use 64×64 + TextureWrapMode.Clamp. **Why:** User reported white lines appearing at screen center in all scenes. **How to apply:** Any code-generated placeholder sprite for full-screen quads (BlackUnderlay, SubtitleBar) must use sufficiently large textures.
- [2026-08-29 22:51:57] User does NOT want to re-run prefab export tools or regenerate scenes when migrating code-built UI to prefab-based instantiation. **Why:** Regenerating scenes loses manual Inspector tweaks; re-exporting prefabs is disruptive during iteration. **How to apply:** When migrating any component to prefab, always keep a runtime code fallback (e.g. if _serializedField==null, build UI in code) so existing scenes continue to work without re-generation. The prefab path is opt-in, not mandatory.
- [2026-08-29 23:10:22] Prefab exporters must create FIXED slot counts, not derive from a single asset's data. **Why:** CookingPrefabExporter used LoadFirstRecipeConfig() which returned CucumberSaladRecipe (3 ingredients) instead of TomatoEggRecipe (4), producing only 3 MotherIngredientSlots in the prefab. Runtime code expects 4 slots. **How to apply:** Always hardcode the maximum expected slot count in exporter loops (e.g. `for i < 4`), and use the asset with the most items for sprite population. Same applies to seasoning slots (`for i < 2`).
- [2026-08-29 23:26:32] Direct prefab YAML editing is the preferred method for adding new UI elements to existing prefabs when user doesn't want to re-export. **Why:** User repeatedly says "我不想导出prefab" / "不想重新生成场景" — re-exporting wipes manual Inspector edits. **How to apply:** When adding new Image/Text fields to a prefab, directly append GameObject+RectTransform+CanvasRenderer+MonoBehaviour blocks to the .prefab YAML file, register them in the parent's m_Children list, and wire them in the CookingView/BaguaView/AlbumView serialized fields. Keep the exporter code in sync too, so future exports won't be wrong.
- [2026-08-29 23:58:35] SessionGameplayCoordinator.FinishMiniGame and CloseMemoryPhotoPreview must NOT synchronously call StartMiniGame/StartDialogue immediately after PublishStableState(). **Why:** PublishStableState synchronously fires MiniGameManager.OnStateChanged → DestroyActiveView → Destroy(oldMiniGame.gameObject). Calling StartMiniGame in the same frame risks creating the new mini-game view before the old one is fully torn down (Destroy is deferred to end-of-frame). User reported getting stuck after collecting photo in cucumber_salad. **How to apply:** Always wrap chained StartMiniGame/StartDialogue calls in StartCoroutine(DelayedAction(...)) — yield one frame — so the old view is destroyed first.

### Project
- [2026-08-30 07:48:34] Local debug network layer (2026-08-27, updated 2026-08-30): LocalDebugService + LocalGameplayBridge + LocalNetworkBootstrap in Network/Local/, auto-registered when FUSION_PRESENT is not defined. Enables single-process Game scene debugging with Tab key role switching. debugSingleProcess flag on SessionGameplayCoordinator bypasses Host authority checks. **Update 2026-08-30:** LocalDebugService.StartHost/StartClient no longer auto-load scenes — they just transition to Connected state and stay on MainMenu. This was causing the room code to flash and disappear because the scene loaded immediately. **Why:** Needed for single-process debugging without Fusion. **How to apply:** This is a debug-only layer; importing Fusion and defining FUSION_PRESENT automatically disables it via conditional compilation.

- [2026-08-30 08:23:47] - [2026-08-27 22:51:32] Bagua mini-game implemented (2026-08-27): Dual mini-game architecture using GameplaySnapshot to unify CookingGameState + BaguaGameState. SessionGameplayCoordinator now routes by miniGameId. Bagua flow: Host triggers via DoorListeningTrigger → Client hears stories + matches items in 3×3 grid → auto-transition to Host photo identification → drag name tags onto photo zones. New intents: StartBaguaMiniGame, MarkBaguaStoryHeard, MatchBaguaItem, AssignBaguaPhotoName. **Why:** Hackathon needs a second mini-game alongside cooking; design requires information asymmetry between Host/Client. **How to apply:** Scene3CSetup now creates BaguaStoryConfig.asset + DoorListeningTrigger in scene. Config has 3 characters (liu_hongxiu/key, liu_hongju/tin_candy_box, liu_hongbin/wooden_cart) + 3 photo zones. Fusion SDK IS imported and FUSION_PRESENT IS defined — FusionSessionService is active.

- [2026-08-27 23:13:56] MiniGameManager.OnStateChanged was using old CookingGameState signature but project had been refactored to GameplaySnapshot (2026-08-27). Fixed to accept GameplaySnapshot and extract .cooking sub-state. LocalGameplayBridge also updated for GameplaySnapshot. **Why:** Project was refactored externally (Bagua added) but MiniGameManager wasn't updated to match. **How to apply:** When IGameplayTransport or SessionGameplayCoordinator changes its state type, check all subscribers (MiniGameManager, LocalGameplayBridge, FusionGameplayBridge) for signature compatibility.
- [2026-08-28 00:51:31] Multi-recipe cooking system added (2026-08-28): CookingMiniGame is fully data-driven from RecipeConfig (seasoningOptions, recipeNote, containerDisplayName, nextRecipeId fields). Two recipes: tomato_egg (reward: photo_hongqiang) → cucumber_salad (reward: photo_hongfang). Chain flow: FinishMiniGame stores _nextRecipeAfterPhoto → returns to Exploration → MemoryAlbum shows photo pickup → Host collects → preview opens → Host closes preview → CloseMemoryPhotoPreview auto-starts next recipe via StartMiniGame. **Why:** Design doc requires sequential dishes where player collects photo reward between dishes. **How to apply:** Add new recipes by creating RecipeConfig assets + wire into coordinator.recipes and MiniGameManager.recipes. Use `Tools > 3C Setup > Update Recipe Assets` to create/update recipe assets and wire them into the open scene without full scene rebuild. RecipeNote (菜谱改痕) shows only on mother's side after cooking completes, never during ingredient selection or on daughter's side.
- [2026-08-28 02:05:34] PlayerInputHandler locks movement when pendingPhotoId or previewPhotoId is non-empty (2026-08-28). **Why:** Design requires player to collect photo rewards before resuming exploration. **How to apply:** If player is stuck after completing a mini-game, check Console for `[PlayerInput] 移动被锁定` log — it means pendingPhotoId is set but the photo pickup button in MemoryAlbumController may not be visible or interactable (e.g. role mismatch in LocalDebug mode where ShowPickup created button with interactable=false and early-return prevented re-enabling). Fixed by updating interactable state on existing pickup when role changes.
- [2026-08-28 21:49:01] [FIXED 2026-08-28] Bug in SessionGameplayCoordinator.FinishMiniGame bagua branch: `State.miniGameId` was set to null BEFORE `FindBaguaConfig(State.miniGameId)` was called, so `_nextDialogueAfterPhoto` for bagua was always null. Fix applied: `FindBaguaConfig(State.miniGameId)` now called before `State.miniGameId = null`.

- [2026-08-28 18:58:31] [2026-08-28] Bagua Client-side UI redesigned (2026-08-28): Replaced nine-grid item matching with flat desktop layout (top: task banner, middle: wooden tray with scattered draggable items, bottom: 3 character cards). Each card has portrait + name/age·title + audio button + dashed drop slot. Must hear story (audio or subtitle fallback) before slot activates. Items scatter by ItemPlacement.anchoredPosition (configured in BaguaStoryConfig). Data model: BaguaStoryEntry lost correctItemId/itemSprite, gained age/title. New ItemPlacement struct (itemId, displayName, sprite, anchoredPosition, isCorrect, characterId) replaces distractorItemIds + correctItemId. 8 items total (3 correct + 5 distractors). Subtitle bar: bottom popup, duration = len/5+1s, audio-present suppresses subtitle. Matched item: scale-pop animation fills slot. Wrong drop: ReturnToOrigin + ShakeRect. No interrupt button on Client side. **Why:** Design doc requires scattered-item drag matching on a nostalgic desktop, not a grid. **How to apply:** Old BaguaStoryConfig.asset deleted — run Tools > 3C Setup > Create Basic Scene to regenerate with new structure. CONTEXT.md created at project root for domain terminology.
- [2026-08-28 21:27:44] [2026-08-28] Album mini-game (game 3) implemented (2026-08-28): Independent third MiniGameBase subclass (AlbumMiniGameView) in MiniGame/Album/. Same-screen co-op — both Host and Client see identical UI and can drag; Host-authoritative via PlaceAlbumSticker/PlaceAlbumNameTag intents (CanRequesterPerform returns true for both roles). Two-phase serial: PlaceStickers (5 stickers → 6 silhouette zones, liu_hongmei/小岩 has no sticker, always empty) → PlaceNameTags (5 name tags → name tag zones on placed stickers) → Complete button → full-screen realistic family portrait (CrossFadeAlpha) → fade to black → GameEnded phase. New GameplayPhase.GameEnded (game body ended, awaiting epilogue content). AlbumTrigger at game start position (0, 0.5) checks collectedPhotoIds contains all 3 required photos (photo_hongqiang, photo_hongfang, bagua_old_family_photo) before allowing entry; shows lockedMessage if not. Clue viewer: left-top "查看线索" button → full-screen panel with photo + yellow sticky note (clueText from AlbumConfig). No hint system (oral communication only). AlbumConfig has 6 AlbumCharacterEntry (characterId, displayName, stickerSprite, photoSprite, clueText, stickerZone/NameTagZone positions+sizes, hasSticker). AlbumGameState: placedStickerCharacterIds, placedNameTagCharacterIds, completed. Bagua game unchanged (HostIdentifyPeople stays). Pre-existing BaguaMiniGameView KeyValuePair==null bug also fixed. **Why:** Design requires a terminal album assembly game using photos collected from cooking+bagua, with 小岩's empty silhouette leading to epilogue. **How to apply:** Run Tools > 3C Setup > Create Basic Scene to regenerate scene with AlbumConfig.asset + AlbumTrigger. Epilogue flow (BlackScreen+text+AIGC video) is a reserved interface — FinishMiniGame sets phase=GameEnded, MiniGameManager shows black overlay.
- [2026-08-28 21:41:48] [project] 4-scene orchestration code changes (2026-08-28): SessionGameplayCoordinator + MiniGameManager now use DontDestroyOnLoad for cross-scene persistence. Coordinator uses SceneManager.sceneLoaded to rebuild dependencies (MemoryAlbumController, DialogueController) after scene transitions. New SceneNames.cs constants class (Core/). Cross-scene transition: CloseMemoryPhotoPreview checks _cookingChainCompleted flag → TransitionToScene(BaguaScene) with Build Settings existence guard. GameManager.HandleSceneLoaded cleans up persistent objects when returning to MainMenu. LocalDebugService.StartHost/StartClient no longer hard-loads Game scene if already in a game scene. FusionSessionService loads Intro (fallback to Cooking) on connect. **Why:** Preparing for 4-scene split (MainMenu/Intro/CookingScene/BaguaScene) without touching .unity files yet. **How to apply:** When creating the actual scene files, update SceneNames.Cooking constant from "Game" to "CookingScene" and ensure BaguaScene is in Build Settings — the TransitionToScene guard will auto-activate.
- [2026-08-28 21:49:01] [2026-08-28] FusionGameplayBridge signature mismatch FIXED (2026-08-28): `BroadcastState(CookingGameState)` → `BroadcastState(GameplaySnapshot)` to match IGameplayTransport interface. `RpcApplyState` deserialization changed from `CookingGameState` to `GameplaySnapshot`. Removed unused `using DoNotForgetMe.MiniGame.Cooking`. Still under `#if FUSION_PRESENT` (not compiled in LocalDebug mode).
- [2026-08-30 07:40:25] PhotonAppSettings.asset: AppIdFusion and AppIdRealtime are BOTH set to `ecb11ee8-d468-495d-bb7f-6c6efd8a1242`. FixedRegion is empty (auto). FUSION_PRESENT IS defined in ProjectSettings.asset Standalone scripting defines (line 609). FusionSessionService.cs compiles and is active (FusionNetworkBootstrap registers it at runtime). **How to apply:** Fusion online mode is ready — no need to fill AppId or define FUSION_PRESENT.


- [2026-08-28 23:23:55] [project] 3-scene split completed (2026-08-28): SceneNames constants changed from "Game" to "LivingRoom"/"Kitchen"/"Courtyard". New files: SceneSetupBase.cs (shared static helpers for all scene generators), LivingRoomSceneSetup.cs (Tools > 3C Setup > Create Living Room Scene), KitchenSceneSetup.cs, CourtyardSceneSetup.cs. Each scene: Ground(20w) + air walls(x=±11, invisible BoxCollider2D Layer=Ground) + camera bounds(constrainX=true, minX=-5, maxX=5). LivingRoom creates GameManager+Coordinator+MiniGameManager (DontDestroyOnLoad carries to Kitchen/Courtyard). Kitchen/Courtyard only have Player+Camera+EventSystem+their triggers. LivingRoom: DeskViewController(x=2)+DoorToKitchen(x=8). Kitchen: MiniGameTrigger(x=3)+DoorToLivingRoom(x=-8). Courtyard: DoorListeningTrigger(x=3)+DoorToLivingRoom(x=-8). Original Scene3CSetup/Create Basic Scene (Game.unity) preserved as single-scene fallback.


- [2026-08-28 23:24:02] [project] 5-scene orchestration with album return (2026-08-28): Flow is MainMenu → Intro → LivingRoom → Kitchen → Courtyard → LivingRoom(album) → GameEnded. Two transition types: (1) Passive — Coordinator _cookingChainCompleted → Courtyard, _baguaChainCompleted → LivingRoom, both via CloseMemoryPhotoPreview. (2) Active — SceneTransitionTrigger (IInteractable) for door F-key transitions. Files: IntroCutsceneController.cs (black screen + monologue + AIGC video + reversal flash → SceneLoader.Load(LivingRoom)), SceneLoader.cs (unified scene load with same-scene guard + Build Settings check), SceneTransitionTrigger.cs (F-key → fade black → SceneLoader.Load). SceneNames constants now point to actual scene names (LivingRoom/Kitchen/Courtyard) — previously all pointed to "Game". IntroSceneSetup.cs (Tools > Create Intro Scene) generates Intro.unity. LocalDebugService.StartHost loads Intro (fallback Kitchen). DeskViewController.cs — first-person desk view on scene load, X/Escape/click X button to exit, F to re-interact (photos collected → album game; not collected → desk view). 3-scene split done: SceneSetupBase.cs shared helpers + LivingRoomSceneSetup/KitchenSceneSetup/CourtyardSceneSetup generate 3 independent .unity files with air walls (x=±11) + camera bounds (constrainX, minX=-5, maxX=5).
- [2026-08-29 21:02:22] Photo reward flow (updated 2026-08-29): Mini-game photo collection uses a new CollectMiniGamePhoto intent (not direct collectedPhotoIds.Add + FinishMiniGame). Flow: Host clicks collect → CollectMiniGamePhoto intent → coordinator adds to collectedPhotoIds + PublishStableState → MemoryAlbumController.Render detects new photo → spawns clone sprite fly-back animation (arc+rotation+scale, 0.7s EaseOutCubic) from screen center to album slot → slot pop (EaseOutBack 0.3s) → Host calls FinishMiniGame. State-first timing (state updates immediately, animation is visual feedback). **Why:** Design requires photo fly-back animation from screen center to top-left album. **How to apply:** MemoryAlbumController Canvas sortingOrder is 150 (above MiniGame Canvas 100) so album + fly clone render on top. Album visible during both Exploration and MiniGame phases. InputBlocker (transparent full-screen Image on album Canvas) freezes all clicks during flight.

- [2026-08-29 18:46:06] [project] Cooking mini-game Prefab migration (2026-08-29): Replaced code-generated UI with Prefab + CookingView binding pattern. New files: CookingView.cs (pure [SerializeField] fields), CookingPrefabExporter.cs (Editor tool: Tools > MiniGame > Export Cooking Prefab), rewritten CookingMiniGame.cs (Resources.Load + Instantiate + in-place Render). Prefab saved to Resources/MiniGamePrefabs/CookingView.prefab. Dual-end panel (MotherPanel/DaughterPanel) with SetActive switching. IngredientSlotView sub-component for repeated drag slots. Factory methods in MiniGameBase NOT deleted (Bagua/Album still use them). Verification doc: docs/cooking-prefab-verification.md. **Why:** Enable visual prefab editing in Unity editor for designers. **How to apply:** Run export tool first to generate prefab, then Play to verify. Bagua and Album need same migration pattern when ready.
- [2026-08-29 19:11:42] [project] Bagua + Album mini-games Prefab migration (2026-08-29): Same pattern as cooking — View script + prefab + in-place Render. BaguaView.cs (dual-end: ClientPanel for desktop item matching, HostPanel for photo identification), BaguaPrefabExporter.cs (Tools > MiniGame > Export Bagua Prefab), rewritten BaguaMiniGameView.cs. AlbumView.cs (single-screen co-op: sticker zones + nametag zones + clue panel + complete animation), AlbumPrefabExporter.cs (Tools > MiniGame > Export Album Prefab), rewritten AlbumMiniGameView.cs. All three mini-games now use prefab + View pattern. Factory methods in MiniGameBase still not deleted (kept for backward compat). **Why:** Enable visual prefab editing for all mini-games. **How to apply:** Run each export tool (Cooking/Bagua/Album) to generate prefabs in Resources/MiniGamePrefabs/.
- [2026-08-29 19:30:10] [project] Prefab migration bug fixes (2026-08-29): (1) CollectButton onClick never wired — _dragCallbacksWired was set true in StartGame, blocked _collectButtonWired check. Fix: separate _collectButtonWired flag. (2) Wrong seasoning could be dropped into dish — missing _recipe.IsCorrectSeasoning() check in drag callback. (3) Bagua photo zones inactive after HidePhotoView — RenderPhotoView never called SetActive(true) on zones. Fix: add zone.root.SetActive(true) in render loop. (4) Bagua shared Background (white Image) was always visible — added SetActive(false) in SetAllInactive. (5) Bagua clientComplete branch skipped UpdateCharacterCards — third card never showed matched state. Fix: call UpdateCharacterCards before HideAllDesktopItems. (6) CourtyardEavesdropView "进入偷听" button reappeared after bagua completion — added _baguaCompleted flag checked via collectedPhotoIds.Contains("bagua_old_family_photo"). (7) PhotoZoneConfig? nullable struct access errors — use .Value for non-nullable context, ?. for nullable. **Why:** Prefab migration introduced in-place render pattern that missed edge cases the old destroy-and-rebuild pattern handled implicitly. **How to apply:** When adding new mini-game prefab migrations, check all state transition branches (completed, clientComplete, step changes) call the appropriate Update methods before hiding elements.
- [2026-08-30 06:28:48] [project] Intro/Ending flow simplified + video integration (2026-08-30): IntroCutsceneController trimmed to 2 stages: 片头视频(片头.mp4) → S1.3 书桌记忆(物件占位+内心独白+"前往厨房"按钮) → LivingRoom. Removed S1.1(独白+AIGC视频) and S1.2(时钟动画) entirely. EndingController simplified to: 黑屏字幕(3句公益) → 片尾视频(片尾.mp4) → 结束. Removed all EndingAct multi-act structure, background image, speaker text. Both videos use RenderTexture+RawImage, clickable/keyable skip, 120s max. Videos auto-loaded via #if UNITY_EDITOR from Assets/_Project/Video/. MiniGameManager gains outroVideoClip field, passes to EndingController via SerializedObject. AlbumMiniGameView.ShowFamilyPortraitAndFinish now pops 微信图片_20260830062314_1474_3230 + child layer (1) with EaseOutBack scale animation before fading to black and calling FinishMiniGame. Album complete flow: click完成 → hide UI → pop portrait layers → 3s hold → black fade → FinishMiniGame → DLG_Ending → GameEnded → EndingController(字幕→片尾视频).

- [2026-08-29 21:59:23] [project] Player & Ground Prefab migration (2026-08-29): New PlayerGroundPrefabExporter.cs (Tools > Scene > Export Player & Ground Prefabs) generates Player.prefab + Ground.prefab in Resources/ScenePrefabs/. SceneSetupBase.CreatePlayer() and CreateGround() now use PrefabUtility.InstantiatePrefab instead of code-generating GameObjects. LivingRoomSceneSetup/KitchenSceneSetup simplified — CreatePlayer() takes no params (all config baked into prefab). Ground width still set in code (CreateCinematicBackground passes bgWorldWidth to localScale.x). Player/Ground position/scale now editable directly in Prefab — code does NOT override Transform values. **Why:** Enable visual editing of player size and ground position in Unity Inspector. **How to apply:** Run Tools > Scene > Export Player & Ground Prefabs first, then re-run scene setup tools. Edit Prefab directly for size/position changes.
- [2026-08-29 21:59:23] [project] SubtitleBar refactored to SubtitleSystem hierarchy (2026-08-29): Old: SubtitleBar (CinematicSubtitle + AudioSource, always visible). New: SubtitleSystem (always active, CinematicSubtitle + AudioSource) → SubtitleBar (hidden by default, black bar + SpriteRenderer) → SubtitleText (hidden by default, TMP). Show() activates SubtitleBar, Hide() deactivates it. AudioSource moved to SubtitleSystem so SubtitleBar no longer shows 🔊 gizmo in Scene view. CinematicSubtitle uses transform.Find("SubtitleBar") to locate children (works on inactive objects). **Why:** User wanted SubtitleBar/SubtitleText hidden when not in use, and the AudioSource 🔊 icon removed from SubtitleBar. **How to apply:** Re-run scene setup tools to regenerate scenes with new hierarchy. BaguaPrefabExporter's internal SubtitleBarRoot/SubtitleText are separate (prefab-internal), not affected by this change.
- [2026-08-29 22:23:10] [project] CinematicSubtitle no longer creates local AudioSource (2026-08-29): Audio playback moved to AudioManager.PlayClip/StopClip (new methods on AudioManager). CinematicSubtitle.Awake now destroys any legacy AudioSource on its GameObject. Scene triggers (Door, MiniGameTrigger, DeskViewController, DoorListeningTrigger) now have semi-transparent colored placeholder sprites (yellow/green/red/blue at 0.3 alpha) so they're visible in the Unity editor — previously SpriteRenderer had no sprite assigned, making them invisible. **Why:** User couldn't see trigger positions in editor; 🔊 gizmo appeared on SubtitleBar at runtime. **How to apply:** Re-run scene setup tools to get visible triggers. Gizmos button in Game view toggles editor debug icons (camera outline, audio listener icon) — not game bugs.
- [2026-08-29 23:10:25] [project] MiniGameTrigger + CourtyardEavesdropView debug fallback (2026-08-29): Both now have EnsureDebugManagers() — when MiniGameManager.Instance is null (direct scene open without LivingRoom DontDestroyOnLoad), auto-creates Coordinator + MiniGameManager + LocalGameplayBridge + GameManager with all config assets loaded via AssetDatabase.FindAssets. Same pattern as CourtyardEavesdropView.EnsureDebugManagers. **Why:** User debugs Kitchen/Courtyard scenes directly without going through LivingRoom. **How to apply:** Any IInteractable trigger that depends on MiniGameManager.Instance should have this fallback.
- [2026-08-30 07:58:20] Fusion integration status: (1) FUSION_PRESENT defined for Standalone + Android + iOS + WebGL. (2) FusionSessionService: SimulationBehaviour + INetworkRunnerCallbacks (NOT override — Fusion 2.x SimulationBehaviour doesn't have virtual callbacks). Host stays on MainMenu, loads Intro via OnPlayerJoined when SessionInfo.PlayerCount >= 2. (3) MainMenuController.OnError no longer clears roomCodeDisplay; Enter key submits room code via onEndEdit. (4) PhotonAppSettings.asset AppIds populated. (5) MainMenu.unity has Camera + Canvas localScale fixed. (6) LocalDebugService.StartHost/StartClient no longer auto-load scenes. (7) Tab key role switch removed from LocalGameplayBridge. (8) SceneLoader.Load now checks for running NetworkRunner — Host uses runner.LoadScene (syncs to Client), Client skips (waits for Fusion auto-sync), falls back to SceneManager.LoadScene if no runner. **How to apply:** To test two-player: build exe (Host) + Editor Play (Client) or two exes. Host creates session → room code shown → Client joins → both load Intro → scenes sync via Fusion.
- [2026-08-30 08:23:41] [2026-08-30 08:25] Fusion runtime bridge injection architecture (2026-08-30): Root cause of "Client 画面卡住" — saved scenes (LivingRoom.unity etc.) were generated WITHOUT FUSION_PRESENT, so they contain LocalGameplayBridge + debugSingleProcess=true, NOT FusionGameplayBridge + NetworkObject. Coordinator._transport stays null → BroadcastState/SendIntent are no-ops → Client receives nothing. Fix (chosen via grilling session, user refused scene regeneration because of manual edits): (1) SessionGameplayCoordinator exposes `static event Action<SessionGameplayCoordinator> OnTransportNeeded`, fired in EnsureDependencies() when _transport==null && Service.IsAvailable. (2) FusionSessionService subscribes after StartGame success → runner.Spawn() the FusionGameplayBridge prefab from Resources/NetworkPrefabs/ (needs manual Tools > Fusion > Create Bridge Prefab run once — editor must be open, batch mode failed due to lockfile). (3) FusionGameplayBridge.TryRegister retries via Invoke 0.2s × max 10 if Coordinator.Instance is null. (4) FusionSessionService.OnSceneLoadDone destroys lingering LocalGameplayBridge (removes Error log + debugSingleProcess bypass). **Why:** Scenes can't be regenerated (user's manual edits), so the network bridge must be runtime-injected via Spawn instead of scene-placed. **How to apply:** After running Tools > Fusion > Create Bridge Prefab once, restart editor + Play to test. If Client still frozen, check: (a) prefab exists at Resources/NetworkPrefabs/, (b) Console shows "[FusionSessionService] FusionGameplayBridge 已 spawned", (c) LocalGameplayBridge destroyed log appears.

### Reference
- [2026-08-30 07:48:32] Tuanjie Editor 1.6.12 installed at E:\Software\Tuanjie\Hub\Editor\2022.3.61t13\Editor\Tuanjie.exe. Batch mode (PowerShell): `& "E:\...\Tuanjie.exe" -batchmode -nographics -projectPath <path> -logFile <path> -quit` — note the `&` call operator is required in PowerShell. **Gotcha 1:** if code changes trigger recompilation during batch mode, assembly reload causes -quit to fire before executeMethod runs. Run twice: first to compile, second to execute. **Gotcha 2:** batch mode fails with "Fatal Error! It looks like another Tuanjie instance is running" if the Editor GUI is already open — close it first (check UnityLockfile in <project>/Temp/). **Gotcha 3:** scripting define symbols in ProjectSettings.asset are per-platform (Standalone, Android, iOS, WebGL etc.) — Editor uses the defines of the active build target, not Standalone. If FUSION_PRESENT is only on Standalone but build target is Android, it won't be defined. Adding defines for ALL platforms is required. Editor restart needed after editing ProjectSettings.asset.

- [2026-08-28 17:31:31] [reference] 程序交付文档位于 docs/程序交付文档.md (created 2026-08-28)。覆盖架构、56脚本清单、数据驱动配置、场景结构、游戏流程、网络层、存档系统、运行验证、缺失资源与接入指南。
